# Ejemplo de Change Request
# Este archivo estaría en .project-spec/changes/CR-001.yaml

schema: cr/v1
id: CR-001
status: proposed

proposed_at: 2025-05-03
author: tano

domain: core
summary: "Implementar CLI básico con comandos init y validate"
description: |
  Crear la estructura de CLI con los comandos fundamentales:
  - spec init: Inicializa .project-spec/
  - spec validate: Valida archivos YAML contra schemas

affects:
  entities: [cli, config]
  files: [src/cli/*, src/schemas/*]
  apis: []

acceptance_criteria:
  - "spec init crea la estructura de directorios"
  - "spec validate retorna error si YAML inválido"
  - "Tests con >80% coverage"

relationships:
  depends_on: []
  affects_decision: []

implementation:
  branch: null
  pr: null
  commits: []
